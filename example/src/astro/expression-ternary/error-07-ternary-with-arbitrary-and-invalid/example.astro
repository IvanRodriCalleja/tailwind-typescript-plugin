---
// ❌ Invalid: Ternary with mix of arbitrary and invalid
// @invalidClasses [invalid-size]
// @validClasses [h-[50vh], h-[30vh]]

const isActive = true;
---

<div class={isActive ? 'h-[50vh] invalid-size' : 'h-[30vh]'}>
	Ternary with arbitrary and invalid
</div>
