---
// ⚠️ Warning: Duplicate in base string
// @duplicateClasses [flex, flex]

import { tv } from 'tailwind-variants';

const button = tv({
		base: 'flex flex items-center'
	});
---

<button class={button()}>Duplicate in base</button>
