---
// ✅ Valid: class property with valid Tailwind classes
// @validClasses [bg-pink-500, hover:bg-pink-700]

import { tv } from 'tailwind-variants';

const button = tv({
---

<button class={button({ color: 'secondary', class: 'bg-pink-500 hover:bg-pink-700' })}>
	Valid Override
</button>
