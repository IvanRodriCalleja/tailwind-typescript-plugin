---
// ✅ Valid: Empty class property should not error
// @validClasses []

import { tv } from 'tailwind-variants';

const button = tv({
---

<button class={button({ color: 'primary', class: '' })}>Empty Override</button>
