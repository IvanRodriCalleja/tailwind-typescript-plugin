---
// ❌ Invalid: Object with string literal key, invalid class
// @invalidClasses [invalid-class]
// @validClasses [flex]

import clsx from 'clsx';
---

<div class={clsx({ flex: true, 'invalid-class': true })}>Invalid string key</div>
