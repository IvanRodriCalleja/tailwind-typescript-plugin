---
// ✅ Valid: Mix of object and static strings
// @validClasses [flex, items-center, justify-center, bg-blue-500]

import clsx from 'clsx';

const isActive = true;
---

<div
	class={clsx('flex', { 'items-center': true, 'justify-center': isActive }, 'bg-blue-500')}>
	Mixed with static
</div>
