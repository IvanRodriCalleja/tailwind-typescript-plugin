---
// ❌ Invalid: Computed property name with invalid class
// @invalidClasses [invalid-class]
// @validClasses [flex]

import clsx from 'clsx';

const isActive = true;
---

<div class={clsx({ ['flex']: true, ['invalid-class']: isActive })}>Computed invalid</div>
