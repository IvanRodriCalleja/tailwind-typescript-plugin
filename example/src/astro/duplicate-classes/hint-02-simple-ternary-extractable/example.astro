---
// 💡 Hint: Simple ternary without utility function
// @extractableClasses [flex, flex]

const isActive = true;
---

<div class={isActive ? 'flex bg-blue-500' : 'flex bg-gray-500'}>
	Simple ternary extractable
</div>
