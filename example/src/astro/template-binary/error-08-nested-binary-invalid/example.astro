---
// ❌ Invalid: Nested binary expressions with invalid class
// @invalidClasses [invalid-nested]
// @validClasses [flex]

const isError = false;
const isActive = true;
---

<div class={`flex ${isError && isActive && 'invalid-nested'}`}>
	Nested binary with invalid
</div>
