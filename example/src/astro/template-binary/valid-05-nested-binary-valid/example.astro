---
// ✅ Valid: Nested binary expressions
// @validClasses [flex, text-red-500, font-bold]

const isError = false;
const isActive = true;
---

<div class={`flex ${isError && isActive && 'text-red-500 font-bold'}`}>
	Nested binary expressions
</div>
