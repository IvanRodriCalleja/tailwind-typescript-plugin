---
// ✅ Valid: Binary with Tailwind variants
// @validClasses [flex, hover:bg-blue-500, md:text-lg]

import clsx from 'clsx';

const isActive = true;
---

<div class={clsx('flex', isActive && 'hover:bg-blue-500 md:text-lg')}>
	Binary with variants
</div>
