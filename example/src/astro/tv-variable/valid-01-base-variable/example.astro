---
// ✅ Valid: Variable with valid classes in base
// @validClasses [flex, items-center]

import { tv } from 'tailwind-variants';

const validBase = 'flex items-center';
const button = tv({
		base: validBase
	});
---

<button class={button()}>Valid Base Variable</button>
