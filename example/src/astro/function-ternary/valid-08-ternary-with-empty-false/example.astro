---
// ✅ Valid: Ternary with empty string in false branch
// @validClasses [flex, bg-blue-500]

import clsx from 'clsx';

const isActive = true;
---

<div class={clsx('flex', isActive ? 'bg-blue-500' : '')}>Empty false branch</div>
