---
// ✅ Valid: Ternary with null/undefined (ignored)
// @validClasses [flex, bg-blue-500]

import clsx from 'clsx';

const isActive = true;
---

<div class={clsx('flex', isActive ? 'bg-blue-500' : null)}>Ternary with null</div>
