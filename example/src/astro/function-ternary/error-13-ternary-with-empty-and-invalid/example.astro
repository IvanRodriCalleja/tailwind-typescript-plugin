---
// ❌ Invalid: Ternary with invalid in non-empty branch
// @invalidClasses [invalid-class]
// @validClasses [flex]

import clsx from 'clsx';

const isActive = true;
---

<div class={clsx('flex', isActive ? 'invalid-class' : '')}>Invalid with empty</div>
