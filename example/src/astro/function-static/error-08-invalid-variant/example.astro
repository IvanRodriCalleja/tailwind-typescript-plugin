---
// ❌ Invalid: Function with invalid variant
// @invalidClasses [invalid-variant:bg-blue]
// @validClasses [flex]

import clsx from 'clsx';
---

<div class={clsx('flex', 'invalid-variant:bg-blue')}>Function with invalid variant</div>
