---
// ✅ Valid: Spread operator with valid classes in function call
// @validClasses [flex, items-center, p-4, m-2]

import { cn } from '../utils';

const baseClasses = ['flex', 'items-center'];
---

<div class={cn(...baseClasses, 'p-4', 'm-2')}>Spread in cn()</div>
