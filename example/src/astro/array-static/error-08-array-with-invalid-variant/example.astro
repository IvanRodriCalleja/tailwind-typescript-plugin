---
// ❌ Invalid: Array with invalid variant
// @invalidClasses [invalid-variant:bg-blue]
// @validClasses [flex]

import { clsx as cn } from 'clsx';
---

<div class={cn(['flex', 'invalid-variant:bg-blue'])}>Array with invalid variant</div>
