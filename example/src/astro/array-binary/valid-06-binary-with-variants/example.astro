---
// ✅ Valid: Binary with Tailwind variants in array
// @validClasses [flex, hover:bg-blue-500, md:text-lg]

import { clsx as cn } from 'clsx';

const isActive = true;
---

<div class={cn(['flex', isActive && 'hover:bg-blue-500 md:text-lg'])}>
	Binary with variants
</div>
