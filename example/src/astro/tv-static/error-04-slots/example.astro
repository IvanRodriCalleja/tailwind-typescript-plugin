---
// ❌ Invalid: Slots with invalid class
// @invalidClasses [invalid-slot-class]
// @validClasses [mr-2]

import { tv } from 'tailwind-variants';

const component = tv({
		slots: {
			base: 'flex',
			icon: 'invalid-slot-class mr-2'
		}
	});
---

<span class={component().icon}>Invalid Slot</span>
