---
// ❌ Invalid: Deeply nested arrays with invalid
// @invalidClasses [invalid-deep]
// @validClasses [flex, items-center, justify-center]

import { clsx } from 'clsx';
---

<div class={clsx({ flex: [[['items-center', 'invalid-deep', 'justify-center']]] })}>
	Invalid deep
</div>
