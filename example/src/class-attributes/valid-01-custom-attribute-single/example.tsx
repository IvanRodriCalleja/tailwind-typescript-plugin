/**
 * ✅ Valid: Custom attribute with single valid class
 * Uses 'containerStyles' as a custom class attribute
 * @validClasses [flex]
 */
export function CustomAttributeSingle() {
	return <div containerStyles="flex">Custom attribute with single class</div>;
}
