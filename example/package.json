{
  "name": "plugin-test-project",
  "version": "1.0.0",
  "description": "Test project for the custom TypeScript plugin",
  "private": true,
  "scripts": {
    "setup": "npm install && cd .. && npm install && npm run build",
    "test": "jest",
    "test:ci": "jest --runInBand",
    "test:shard": "jest --shard",
    "lint": "eslint src --ext .ts,.tsx,.json --quiet --cache",
    "tsc": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/react": "^19.2.5",
    "@vue/language-core": "^3.2.1",
    "@vue/typescript-plugin": "^2.2.0",
    "jest": "^29.7.0",
    "tailwind-typescript-plugin": "file:..",
    "ts-jest": "^29.4.5",
    "typescript": "^5.7.2",
    "vue": "^3.5.13"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "classnames": "^2.5.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.4.0",
    "tailwind-variants": "^3.2.2",
    "tailwindcss": "^4.1.17"
  }
}
